chain = {{ .Chain }}

[pbs]
port = {{ .Port }}
host = "0.0.0.0"

rpc_url = "{{ .ExecutionApi }}"

{{ range $index, $relay := .Relays }}
[[relays]]
id = "mev_relay_{{$index}}"
url = "{{ $relay }}"
{{- end }}
