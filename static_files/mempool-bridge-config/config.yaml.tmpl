addr: ":{{ .ListenPortNum }}"

{{- if .SourceEndpoints }}
source:
  nodeRecords:
{{- range $endpoint := .SourceEndpoints }}
    - "{{ $endpoint }}"
{{- end }}
{{- end }}

target:
  nodeRecords:
{{- range $endpoint := .TargetEndpoints }}
    - "{{ $endpoint }}"
{{- end }}
