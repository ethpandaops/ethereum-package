participants:
  - el_type: geth
    cl_type: lighthouse
    cl_extra_params: ["--target-peers=0"]

  - el_type: geth
    cl_type: prysm
    checkpoint_sync_enabled: true
    skip_start: true
    validator_count: 1
network_params:
  preset: minimal
  seconds_per_slot: 2
additional_services:
  - dora

checkpoint_sync_url: "http://cl-1-lighthouse-geth:4000"
