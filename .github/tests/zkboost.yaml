participants:
  - count: 2
    el_type: reth
    cl_type: lighthouse

additional_services:
  - zkboost
  - dora

zkboost_params:
  zkvms:
    - kind: mock
      proof_type: reth-zisk
      mock_proving_time_ms: 5000
      mock_proof_size: 1024
    - kind: mock
      proof_type: ethrex-zisk
      mock_proving_time_ms: 5000
      mock_proof_size: 1024
