participants:
  - count: 2
    el_type: reth
    cl_type: lighthouse

additional_services:
  - zkboost
  - dora
  - prometheus
  - grafana
  - tempo

zkboost_params:
  zkvms:
    - kind: mock
      proof_type: reth-zisk
    - kind: mock
      proof_type: ethrex-zisk
      mock_proving_time: { kind: random, min_ms: 2000, max_ms: 8000 }
