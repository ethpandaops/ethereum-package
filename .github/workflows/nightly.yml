name: Nightly test Workflow

on:
  schedule:
    - cron: "0 2 * * *" # This schedules the workflow to run at 02:00 UTC every day

jobs:
  run_with_args:
    strategy:
      matrix:
        file_name:
          [
            "./.github/tests/besu-all.yaml",
            "./.github/tests/erigon-all.yaml",
            "./.github/tests/ethereumjs-all.yaml",
            "./.github/tests/geth-all.yaml",
            "./.github/tests/lighthouse-all.yaml",
            "./.github/tests/lodestar-all.yaml",
            "./.github/tests/mix.yaml",
            "./.github/tests/nimbus-mev.yaml",
            "./.github/tests/nethermind-all.yaml",
            "./.github/tests/nimbus-all.yaml",
            "./.github/tests/prysm-all.yaml",
            " ./.github/tests/reth-all.yaml",
            "./.github/tests/teku-all.yaml",
            "./.github/tests/parallel-keystores-1.yaml",
            "./.github/tests/parallel-keystores-2.yaml",
            "./.github/tests/parallel-keystores-3.yaml",
            "./.github/tests/disable-peer-scoring.yaml",
            "./.github/tests/petra.yaml",
          ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Kurtosis
        run: |
          echo "deb [trusted=yes] https://apt.fury.io/kurtosis-tech/ /" | sudo tee /etc/apt/sources.list.d/kurtosis.list
          sudo apt update
          sudo apt install kurtosis-cli
          kurtosis analytics disable

      - name: Run Starlark
        run: kurtosis run ${{ github.workspace }} --args-file ${{ matrix.file_name }}
